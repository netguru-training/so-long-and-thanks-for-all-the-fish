%div.container
  = render place
  %h3= t('reviews.headers.add_new')
  = render 'reviews/form', place_id: place.id
  %h3= t('reviews.headers.list')
  = render partial: 'reviews/review', collection: place.reviews

